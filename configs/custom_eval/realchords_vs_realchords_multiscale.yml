base_model: "/hpcwork/thes2192/realchords/logs/my_logs/decoder_only/decoder_only_online_chord/step=11000.ckpt"
model:
  - "realchords=/hpcwork/thes2192/realchords/logs/my_logs/rl/realchords/actor.pth"
  - "realchords_multiscale=/hpcwork/thes2192/realchords/logs/my_logs/rl/realchords_multiscale/actor.pth"
seed: 42

dataset_name: hooktheory
dataset_split: test
save_dir: logs/custom_eval/realchords_vs_realchords_multiscale_test
batch_size: 64
num_batches: -1
run_full_songs: false

# base_model + both RL actors are all fine-tuned from/around the same
# hooktheory-only vocab (5902-token embedding table, verified), matching
# this reward checkpoint's own -- Vendi is safe to compute here. Omit this
# field entirely (not just leave it blank) to skip Vendi if you ever swap
# in a checkpoint trained on a different vocab; see
# realchords/utils/custom_evaluation.py's contrastive_checkpoint docstring.
contrastive_checkpoint: "/hpcwork/thes2192/realchords/logs/my_logs/contrastive_reward/contrastive_reward/step=8000.ckpt"
